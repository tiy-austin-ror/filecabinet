<%= form_for(category) do |f| %>

        <% end %>

        <div>
          <%= f.label :name %>
          <%= f.text_field :name %>
        </div>

        <div>
          <%= f.submit %>
        </div>
        <% end %>
